@using HOT.Assessment.RockPaperScissor.Core.Domain.Game
<i class="@GetActionClass()"/>

@code {
    [Parameter]
    public HandAction HandAction { get; set; }

    private string? GetActionClass()
    {
        return HandViewHelper.GetActionClass(HandAction);
    }
}